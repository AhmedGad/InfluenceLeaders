create table userid(
    id BigInt
);

create table graph(
    id1 BigInt,
    id2 BIgInt
);

create table finished (
    id BigInt
);

create table finished2 (
    id BigInt
);


insert into userid value(280089745);
