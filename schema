create table userid(
    id BigInt
);

create table graph(
    id1 BigInt,
    id2 BIgInt
);

create table finished (
    id BigInt
);


delete from userid;
delete from finished;
delete from graph;

insert into userid value(280089745);
